export type Situation = {
  _id: string
  name: string
  slug: string
  description?: string
}

export type QuoteType = {
  _id: string
  name: string
  slug: string
  description?: string
}